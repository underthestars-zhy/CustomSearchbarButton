import XCTest
@testable import CustomSearchbarButton

final class CustomSearchbarButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CustomSearchbarButton().text, "Hello, World!")
    }
}
